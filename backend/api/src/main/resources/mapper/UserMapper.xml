<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aivideo.api.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.aivideo.api.entity.User">
        <id property="userNo" column="user_no"/>
        <result property="loginId" column="login_id"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="birthDate" column="birth_date"/>
        <result property="profileImage" column="profile_image"/>
        <result property="role" column="role"/>
        <result property="tier" column="tier"/>
        <result property="creatorId" column="creator_id"/>
        <result property="googleApiKey" column="google_api_key"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findByLoginId" resultMap="UserResultMap">
        SELECT * FROM users WHERE login_id = #{loginId} AND status = 'ACTIVE'
    </select>

    <select id="findByUserNo" resultMap="UserResultMap">
        SELECT * FROM users WHERE user_no = #{userNo} AND status = 'ACTIVE'
    </select>

    <insert id="insert" parameterType="com.aivideo.api.entity.User" useGeneratedKeys="true" keyProperty="userNo">
        INSERT INTO users (login_id, password, name, email, phone, birth_date, role, status)
        VALUES (#{loginId}, #{password}, #{name}, #{email}, #{phone}, #{birthDate}, #{role}, #{status})
    </insert>

    <!-- v2.9.150: 사용자에게 연결된 크리에이터 ID 조회 -->
    <select id="findCreatorIdByUserNo" resultType="Long">
        SELECT creator_id
        FROM users
        WHERE user_no = #{userNo} AND status = 'ACTIVE'
    </select>

    <!-- v2.9.150: 사용자 크리에이터 ID 업데이트 -->
    <update id="updateCreatorId">
        UPDATE users
        SET creator_id = #{creatorId},
            updated_at = NOW()
        WHERE user_no = #{userNo}
    </update>

    <!-- v2.9.169: 사용자 Google API 키 업데이트 -->
    <update id="updateGoogleApiKey">
        UPDATE users
        SET google_api_key = #{googleApiKey},
            updated_at = NOW()
        WHERE user_no = #{userNo}
    </update>

</mapper>
